import {Modal} from "@mui/material";


function ImportExport(){
    return (
        <Modal>
            {/*
            import => select file

            export json => safe to location => encrypt?
            */}
        </Modal>
    )
}